{
  "name": "amazon-kinesis-stream",
  "version": "1.0.0",
  "description": "This is a sample template for amazon-kinesis-stream - Below is a brief explanation of what we have generated for you:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "./scripts/deploy.sh",
    "logs1": "sam logs -n kvsTriggerFunction --stack-name kvs-stream --tail",
    "logs2": "sam logs -n kvsStreamFunction --stack-name kvs-stream --tail"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.566.0",
    "decoder": "^1.1.6"
  }
}
